<div class="article-wrapper container">
  {{#each article}}
  <h2>{{this.title}}</h2>
  <p>{{this.summary}}
    <span>
      <a target="_blank" href={{this.link}}>here</a>
    </span>
    &laquo;
  </p>
  <div class="note"><i class="fas fa-sticky-note"></i> Add a note</div>
  {{/each}}
</div>